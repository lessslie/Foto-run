/**
 * Barrel export para interfaces de autenticación
 *
 * Permite importar todas las interfaces desde un solo lugar:
 * import { JwtPayload, AuthenticatedUser, RequestWithUser } from './interfaces';
 */

export * from './jwt-payload.interface';
export * from './request-with-user.interface';
